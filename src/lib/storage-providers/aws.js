class Aws {
    isConnected() {
        // return if connected
        // verify if still logged
        /*
        let oauthAccounts = {}
        let storeObject = window.localStorage.getItem('oauthAccounts')
        if (storeObject !== null && storeObject !== undefined) {
            oauthAccounts = JSON.parse(storeObject)
        }
        */
    }
}

export default Aws
